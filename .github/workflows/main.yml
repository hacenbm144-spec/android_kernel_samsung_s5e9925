name: Build tcp_bbr.ko
on:
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-22.04
    timeout-minutes: 90
    steps:
      - name: Checkout kernel source
        uses: actions/checkout@v4
        with:
          repository: ExtremeXT/android_kernel_samsung_s5e9925
          ref: 9925_rebase123
          fetch-depth: 1
      - name: Find defconfig
        run: |
          echo "=== Finding defconfig ==="
          find arch -name "*defconfig*" | grep -i "9925\|s22\|b0s\|universal" | head -20
          echo "=== All configs ==="
          ls arch/arm64/configs/ | head -30
      - name: Install dependencies
        run: sudo apt-get update -y && sudo apt-get install -y bc bison build-essential curl flex git libelf-dev libncurses5-dev libssl-dev python3 zip zlib1g-dev
      - name: Setup Clang r584948
        run: |
          mkdir -p $HOME/toolchain/clang
          curl -L "https://storage.googleapis.com/android-toolbox-public/clang-r584948-linux-x86-64.tar.gz" -o clang.tar.gz && tar -xf clang.tar.gz -C $HOME/toolchain/clang --strip-components=1 || true
          if [ ! -f $HOME/toolchain/clang/bin/clang ]; then
            echo "Falling back to Clang 17..."
            curl -L "https://github.com/llvm/llvm-project/releases/download/llvmorg-17.0.6/clang+llvm-17.0.6-x86_64-linux-gnu-ubuntu-22.04.tar.xz" -o clang.tar.xz
            tar -xf clang.tar.xz --strip-components=1 -C $HOME/toolchain/clang
          fi
          echo "$HOME/toolchain/clang/bin" >> $GITHUB_PATH
          $HOME/toolchain/clang/bin/clang --version
      - name: Upload log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-log
          path: build.log
          retention-days: 7